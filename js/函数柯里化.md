```javascript
function add(a, b, c) {
            return a + b + c;
        }
function myCurrying(fn) {

  return function curry(...args) { 

    if (args.length >= fn.length) {
      return fn.apply(this, args) 
    } else {

      function inner(...arg) {
        return curry.apply(this, [...args, ...arg])

      }
      return inner
    }


  }
}
const _add = myCurrying(add);
console.log(_add(1, 2, 3))
```

